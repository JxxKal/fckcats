# Platzhalter. Die tatsaechliche Konfiguration erzeugt docker-entrypoint.sh
# beim Start, abhaengig davon ob ein Zertifikat in /certs liegt.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
}
